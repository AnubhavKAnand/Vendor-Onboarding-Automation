COLUMN TABLE vendorportal_AgentExtractionLog (
  ID NVARCHAR(36) NOT NULL,
  createdAt TIMESTAMP,
  createdBy NVARCHAR(255),
  modifiedAt TIMESTAMP,
  modifiedBy NVARCHAR(255),
  Vendor_ID NVARCHAR(36) NOT NULL,
  RawLLMOutput NCLOB,
  MissingFields NVARCHAR(500),
  EmailTriggered BOOLEAN DEFAULT FALSE,
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN vendorportal_Vendor AS Vendor ON (Vendor.ID = Vendor_ID)
)